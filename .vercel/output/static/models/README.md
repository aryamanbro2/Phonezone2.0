# 3D Models

Drop your 3D model files here:

- `iphone.glb` — iPhone 3D model
- `samsung.glb` — Samsung 3D model

Supported formats: `.glb` (recommended), `.gltf`

The app will automatically load these when the user toggles between iPhone Lover / Samsung Lover.
